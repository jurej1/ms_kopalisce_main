import 'package:flutter/material.dart';

class QuestionDisplayer extends StatelessWidget {
  const QuestionDisplayer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
