import 'package:flutter/material.dart';

class QuizDisplayer extends StatelessWidget {
  const QuizDisplayer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
